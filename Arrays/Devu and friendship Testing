#include <bits/stdc++.h>
using namespace std;

int main() {
    int T;
    cin >> T;

    while (T--) {
        int n;
        cin >> n;

        set<int> days;

        for (int i = 0; i < n; i++) {
            int x;
            cin >> x;
            days.insert(x);
        }

        cout << days.size() << endl;
    }

    return 0;
}
